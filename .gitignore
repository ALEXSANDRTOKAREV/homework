# # Игнорировать временные файлы Visual Studio, результаты и сборка
# # файлы, созданные дополнениями Visual Studio.
# #
# # Получить последнюю версию с https://github.com/github/gitignore/blob/main/VisualStudio.gitignore

# Пользовательские файлы
* .rsuser
* .суо
* .пользователь
* .userosscache
*  .sln.docstates

# Пользовательские файлы (MonoDevelop/Xamarin Studio)
* .userprefs

# Автоматически генерируемые файлы Mono
моно_краш. *

# Результаты сборки
[ дд ] ebug /
[ Дд ] ebugPublic /
[ Rr ] освободить /
[ Rr ] освобождает /
х64 /
х86 /
[ Ww ][ Ii ][ Nn ] 32 /
[ Аа ] [ Рр ][ Мм ]/
[ Аа ][ Рр ][ Мм ] 64 /
корп. /
[ Бб ] в /
[ Оо ] бж /
[ Ll ] ог /
[ Ll ] огс /

# Visual Studio 2015/2017 каталог кэша/параметров
.vs /
# Раскомментируйте, если у вас есть задачи, которые представляют собой файлы проекта в wwwroot
# wwwрут/

# Автоматически генерируемые файлы Visual Studio 2017
Файлы \ Сгенерированные /

# Результаты теста MSTest
[ Tt ] есть [ Rr ] результат  * /
[ Bb ] уилд [ Ll ] ог. *

# НУнит
* .VisualState.xml
TestResult.xml
нунит- * .xml

# Результаты сборки проекта ATL
[ Дд ] ebugPS /
[ Rr ] eleasePS /
dlldata.c

# Результаты тестов
BenchmarkDotNet.Артефакты /

# .NET ядро
проект.lock.json
проект.фрагмент.lock.json
артефакты /

# Скаффолдинг ASP.NET
ЛесаReadMe.txt

# СтильПолицейский
Стайлкопрепорт.xml

# Файлы, созданные Visual Studio
* _i.c
* _п.с
* _ч.ч
* .илк
* .мета
* .объект
* .iobj
* .pch
* .pdb
* .ipdb
* .pgc
* .пгд
* .рсп
* .sbr
* .tlb
* .tli
* .tlh
* .tmp
* .tmp_proj
* _wpftmp.csproj
* .лог
* .тлог
* .vspscc
* .vsscc
.сборки
* .pidb
* .svclog
* .scc

# Тестовые файлы Наглость
_Наглость *

# Файлы кэша Visual C++
ипч /
* .точки  доступа
* .ncb
* .opendb
* .opensdf
* .sdf
* .файл  кэша
* .VC.db
* .VC.VC.opendb

# Профилировщик Visual Studio
* .psess
* .всп
* .VSpx
* .сап

# Файлы трассировки Visual Studio
* .e2e

# Локальное волшебное пространство TFS 2012
$  тс /

# Инструментарий получения информации
* .gpState

# ReSharper - это надстройка для программирования .NET
* _ReSharper /
. * [ Rr ] e [ Ss ] харпер
* .Настройки  точек.пользователь

# TeamCity - это надстройка для сборки
_TeamCity *

# dotCover - это инструмент для покрытия кода
* .dotCover

# AxoCover — инструмент для покрытия кода
.axoCover / *
! .axoCover / settings.json

# Coverlet — это бесплатный кроссплатформенный инструмент для покрытия кода.
покрытие * .json
покрытие * .xml
покрытие * .info

# Результаты покрытия кода Visual Studio
* .покрытие
* .coveragexml

# NCrunch
_NCrunch_ *
. * хруст * .local.xml
nCrunchTemp_ *

# МогучийЛось
* .мм. *
АвтоТест.Нет /

# Веб-верстак (sass)
.sass-кэш /

# Выходная папка Installshield
[ Ээ ] Экспресс /

# DocProject - это надстройка генератора документации
DocProject / сборкапомощь /
DocProject / Справка / * .HxT
DocProject / Справка / * .HxC
DocProject / Справка / * . hhc
DocProject / Помощь / * .hhk
DocProject / Справка / * .hhp
DocProject / Справка / Html2
DocProject / Справка / html

# Каталог с одним компьютером
опубликовать /

# Опубликовать веб-вывод
. * [ стр ] ublish.xml
* .azurePubxml
# Примечание: прокомментируйте всю строку, если вы хотите проверить настройки веб-развертывания,
# нет подключения к базе данных (с потенциальными паролями) незашифрованными
* .pubxml
* .publishproj

# Веб-приложение Microsoft Azure позволяет настроить взаимодействие. Прокомментируйте эту статью, если хотите
# проверка параметров веб-приложения Azure, но конфиденциальная информация, содержащаяся
# в этих скриптах будут незашифрованы
Публиковать  скрипты /

# Пакеты NuGet
* .nupkg
# Пакеты символов NuGet
* .snupkg
# Папка пакетов может быть проигнорирована из-за восстановления пакета
** /[ Pp ] учетные  записи / *
# кроме build/, который используется в качестве целевого объекта MSBuild.
** ! /[ Pp ] учетные  записи / сборка /
# Раскомментируйте, если необходимо, однако, как правило, он будет восстановлен при необходимости
# !**/[Pp]учетные записи/репозитории.config
Файлы  project.json  # NuGet v3 больше рассмотренных файлов
* .nuget.реквизит
* .nuget.цели

# Вывод сборки Microsoft Azure
csx /
* .сборка.csdef  * .сборка.csdef

# Эмулятор Microsoft Azure
экф /
ркп  (  б  ) /

# Каталоги и файлы пакетов приложений Магазина Windows
Пакеты приложений /
BundleАртефакты /
Пакет.StoreAssociation.xml
_pkginfo.txt
* .приложение
* .appxbundle
* .appxupload

# Кеш-файлы Visual Studio
# файлы, оканчивающиеся на .cache, можно игнорировать
* . [ Копия ] боль
# но следите за каталогами, оканчивающимися на .cache
! ?* . [ Копия ] боль /

# Другие
КлиентБин /
~$ *
* ~
* .dbmdl
* .dbproj.schemaview
* .jfm
* .pfx
* .опубликовать настройки
orleans.codegen.cs

# Включение файлов со строгими именами может представлять угрозу безопасности
# (https://github.com/github/gitignore/pull/2483#issue-259490424)
# *.snk

# Так как есть несколько рабочих процессов, раскомментируйте следующую строку, чтобы игнорировать Bower_components
# (https://github.com/github/gitignore/pull/1529#issuecomment-104372622)
# Bower_components/

# Проекты RIA/Silverlight
Сгенерированный_код /

# Резервное копирование файлов и отчетов о преобразовании старого файла проекта
# на более новую версию Visual Studio. Резервные файлы не нужны,
# потому что у нас есть git ;-)
_UpgradeReport_Files /
Резервная копия * /
Журнал обновлений * .XML
Журнал обновлений * .htm
ServiceFabricBackup /
* .rptproj.бак

# файлы SQL-сервера
* .мдф
* .ldf
* .ндф

# Проекты бизнес-аналитики
* .rdl.данные
* .бим.макет
* .bim_ * .настройки
* .rptproj.rsuser
- * [ Bb ] ackup.rdl
- * [ Bb ] резервная копия  ( [ 0-9 ] ).rdl
- * [ Bb ] ackup  ( [ 0-9 ][ 0-9 ] ).rdl

# Подделки Майкрософт
Поддельные  сборки /

  Файл дополнен  дополнением  # GhostDoc
* .GhostDoc.xml

# Инструменты Node.js для Visual Studio
.ntvs_analysis.dat
узлы_модули /

# Журнал сборки Visual Studio 6
* .плг

# параметры рабочейобласти файла Visual Studio 6
* .опт

# Автоматически сгенерированный файл рабочей области Visual Studio 6 (содержит, какие файлы были открыты и т.д.)
* .vbw

# Автоматически сгенерированный файл проекта Visual Studio 6 (содержит, какие файлы были открыты и т.д.)
* .vbp

# Рабочая область Visual Studio 6 и файл проекта (рабочие файлы проекта, содержащие файлы для включения в проект)
* .dsw
*  .дсп

# Технические файлы Visual Studio 6
* .ncb
* .точки  доступа

# Visual Studio LightSwitch вывод сборки
** / * .HTMLClient / Созданные  статьи
** / * .DesktopClient / Созданные  статьи
** / * .DesktopClient / ModelManifest.xml
** / * .Server / Созданные  статьи
** / * . Сервер / ModelManifest.xml
_Pvt_Extensions

# Менеджер зависимостей пакетов
.пакет / пакет.exe
пакет-файлы /

# ФЕЙК - F# Сделать
.фейк /

# Персональные настройки CodeRush
.cr / личный

# Инструменты Python для Visual Studio (PTVS)
__pycache__ /
* .pyc

# Cake — раскомментируйте, если вы его используете
# инструментов/**
# !tools/packages.config

# Студия вкладок
* .тсс

# Конфигурационный файл JustMock от Telerik
* .jmconfig

# Выходные данные сборки BizTalk
* .btp.cs
* .btm.cs
* .odx.cs
* .xsd.cs

# Результаты анализа пользовательского интерфейса OpenCover
Открытая обложка /

# Выходные данные локального запуска Azure Stream Analytics
ASALocalRun /

# Двоичный и структурированный журнал MSBuild
* .бинлог

# Файл конфигурации отладчика графического процессора NVidia Nsight
* .nvuser

# Рабочая папка MFractors (инструмент повышения производительности Xamarin)
.мфрактор /

# Локальная история для Visual Studio
.локальная  история /

# История файлов Visual Studio (VSHistory)
.вшистория /

# Временная база данных BeatPulse Healthcheck
Healthchecksdb

# Папка резервного предложения для инструмента преобразования ссылок на пакеты в Visual Studio 2017
Резервное  медицинское  обследование /

# Рабочая папка Ionide (кроссплатформенный F # VS Code tools)
.ионид /

# Fody - автоматически сгенерированная XML-схема
FodyWeavers.xsd

# Файлы VS Code для тех, кто работает с несколькими инструментами
.vscode / *
.vscode! /settings.json _
.vscode! /tasks.json _
.vscode! /запуск.json _
.vscode! /расширения.json _
* .code-рабочее  пространство

# Локальная история для кода Visual Studio
.история /

# Файлы установщика Windows из выходных данных сборки
* .cab
* .msi
* .msix
* .msm
*  .MSP

# Гонщик JetBrains
* .sln.iml